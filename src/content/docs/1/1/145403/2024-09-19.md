---
lang: it
title: Lezione (2024-09-19)
---

# Funzioni definite a tratti

Il dominio è diviso in parti, e per ogni parte il grafico è descritto da una
funzione diversa.

**Esempio**:

$$
f(x) = |x| = \begin{cases}
x >= 0 & \implies x  \\
x < 0    & \implies -x
\end{cases}
$$

# Trasformazioni del grafico di una funzione

## Riflessione

- rispetto all'asse $x$: $f(x) -> -f(x)$;
- rispetto all'asse $y$: $f(x) -> f(-x)$;

### Traslazione

- verticale: $f(x) + a$:
  - se $a > 0$: il grafico si sposta verso l'alto;
  - se $a < 0$: il grafico si sposta verso il basso;
- orizzontale: $f(x + a)$:
  - se $a > 0$: il grafico si sposta verso sinistra;
  - se $a < 0$: il grafico si sposta verso destra;

### Riscalamento

- verticale: $k\ f(x)$:
  - se $k > 1$: il grafico si espande;
  - se $k < 1$: il grafico si comprime;
- orizzontale: $f(k\ x)$:
  - se $k > 1$: il grafico si comprime;
  - se $k < 1$: il grafico si espande;

## Inversa di una funzione

Data $f: [0, +oo) -> bb(R)$;

<!-- ![Grafico di una funzione invertibile](uploads/95e5342c-9c9b-4504-8588-8372b290017a/0341ba05-9506-4fdc-bb74-4307e4c07d5f/funzione-invertibile.svg) -->

Il dominio di $f^{-1}(x)$ è dato dal codominio di $f(x)$.

Per trovare l'inversa risolvo l'equazione $x = f(y)$, ed estraggo la y.

<!-- ![Grafico di una funzione inversa](uploads/95e5342c-9c9b-4504-8588-8372b290017a/4f93bfb6-e994-40dd-9d3e-9aa73d3cfead/funzione-inversa.svg) -->

Nel piano cartesiano basta fare la simmetria rispetto alla retta $y = x$.
