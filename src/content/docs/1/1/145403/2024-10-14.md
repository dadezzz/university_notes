---
lang: it
title: Lezione (2024-10-14)
---

# Limiti notevoli

| $$ \lim\_{x -> 0} \frac{sin(x)}{x} = 1

$$
|
|----|
|
$$

\lim\_{x -> 0} \frac{1 - cos(x)}{x^{2}} = \frac{1}{2}

$$
|
|
$$

\lim\_{x -> 0} \frac{tan(x)}{x} = 1

$$
|
|
$$

\lim\_{x -> oo} \left(1 + \frac{1}{x}\right)^{x} = e

$$
|
|
$$

\lim\_{x -> 0} \frac{\ln(1 + x)}{x} = 1

$$
|
|
$$

\lim\_{x -> 0} \frac{e^{x} - 1}{x} = 1

$$
|
|
$$

\lim\_{x -> 0} \frac{a^{x} - 1}{x} = \ln(a)

$$
|
|
$$

\lim\_{x -> 0} \frac{(1 + x)^{a} - 1}{x} = a

$$
|

# Continuità con i limiti di funzione

$f$ è continua se e solo se $\lim_{x -> x_{0}} f(x) = f(x_{0})$.

In generale il valore di un limite in un punto di una funzione non da nessuna indicazione riguardo al valore della funzione in quel punto. In una funzione continua invece, i 2 valori devono corrispondere.

Sia $f: D -> bb(R)$ e $A \subset D$, $f$ è continua in $A$ se $f$ è continua $\forall\ x_{0} in A$, in questo caso si scrive $f in C^{0}(A)$ (ovvero $f$ fa parte dell'insieme delle funzioni continue in $a$). $C^{0}$ indica che la funzione è continua, se fosse $C^{1}$ allora vorrebbe dire che anche la derivata prima della funzione è continua in quell'intervallo.

Tutte le funzioni elementari sono continue (funzione elevamento a potenza, funzioni trigonometriche, funzioni esponenziali, e loro funzioni inverse).

# Tipi di discontinuità

* discontinuità eliminabile: $\exists\ \lim_{x -> x_{0}} f(x) \land \lim_{x -> x_{0}} f(x) != f(x_{0})$
* discontinuità a salto: $\limsup_{x -> x_{0}} f(x) != \liminf_{x -> x_{0}} f(x)$
* discontinuità essenziale: $\nexists\ \liminf_{x -> x_{0}} f(x) \lor \nexists\ \limsup_{x -> x_{0}} f(x)$

# Operazioni tra funzioni continue

Date $f, g: D \subset bb(R) -> bb(R)$ e $f, g in C^{0}(D)$, allora i risultati ottenuti dalle seguenti operazioni sono anch'essi funzioni continue:

* $f(x) \pm g(x)$
* $f(x)\ g(x)$
* $\frac{f(x)}{g(x)}$ (con $g(x) != 0$)
* $f \circ g$ e $g \circ f$
$$
