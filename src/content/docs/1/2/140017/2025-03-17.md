---
description:
  Derivate direzionali, differenziabilità e gradiente. Teorema del differenziale
  totale e condizioni di classe C¹.
lang: it
title: Differenziabilità, derivate direzionali e gradiente
---

## Teorema del differenziale totale

Data una funzione $f$ di più variabili per cui esiste $\nabla f(\mathbf{p}_0)$,
quale proprietà aggiuntiva è necessaria per garantire la differenziabilità di
$f$ in $\mathbf{p}_0$?

Nel caso in cui $\nabla f$ esista, ma le derivate parziali non siano continue,
non si possono trarre conclusioni sulla differenziabilità della funzione,
rendendo necessario ricorrere alla definizione basata sul piano tangente.

:::note

Una funzione si dice di classe $C^n(A)$ se è derivabile $n$ volte su $A$ e la
sua $n$-esima derivata è continua su $A$.

:::

Se $f \in C^1(A)$, allora $f$ è differenziabile in ogni punto di $A$.

## Derivate direzionali

Le derivate parziali costituiscono un caso particolare delle derivate
direzionali, dato che il calcolo è semplice poiché varia solo la coordinata
lungo l'asse di interesse.

Una derivata direzionale è la derivata della funzione lungo una direzione
arbitraria data da un vettore $\mathbf{v}$.

$f$ è direzionalmente derivabile nella direzione $\mathbf{v}$ nel punto
$\mathbf{p}_0$ se:

$$
\frac{\partial f}{\partial \mathbf{v}}(\mathbf{p}_0) = \lim_{h \to 0} \frac{f(\mathbf{p}_0 + h\ \mathbf{v}) - f(\mathbf{p}_0)}{h} \in \R
$$

### Rapporto tra differenziabilità e derivabilità direzionale

Supponendo che $f$ sia differenziabile in $\mathbf{p}_0$, allora, per ogni
direzione $\mathbf{v} \in \R^n$:

$$
\frac{\partial f}{\partial \mathbf{v}}(\mathbf{p}_0) = \nabla f(\mathbf{p}_0) \cdot \mathbf{v} = df(\mathbf{p}_0) \cdot \mathbf{v}
$$
