---
description:
  Definizione dell'integrale triplo su parallelepipedi e insiemi misurabili con
  il teorema del cambio di variabili per funzioni continue limitate e
  misurabili.
lang: it
title: Teoria degli integrali tripli e cambio variabili
---

## Integrali tripli

### Integrale triplo su un parallelepipedo

Sia $Q = [a_1, b_1] \times [a_2, b_2] \times [a_3, b_3] \subset \R^3$ un
parallelepipedo.

Per ciascun lato prendiamo una suddivisione:

- $D_1 = \Set{a_1 = x_0 < \ldots < x_i < \ldots < x_{n_1} = b_1 }$
- $D_2 = \Set{a_2 = y_0 < \ldots < y_i < \ldots < y_{n_2} = b_2 }$
- $D_3 = \Set{a_3 = z_0 < \ldots < z_i < \ldots < z_{n_3} = b_3 }$

Si chiama suddivisione del parallelepipedo $Q$ una suddivisione
$D = D_1 \times D_2 \times D_3$. Quindi $Q$ risulta diviso in $n_1\ n_2\ n_3$
parallelepipedi.

$|Q_{ijk}|_3$ indica il volume del sotto-parallelepipedo $Q_{ijk}$.

![Suddivisione di un volume in $\R^3$](../../../../../images/analisi-2/suddivisione-volume-r3.png)

Definiamo:

- $S(f,D) = \sum_{i, j, k} M_{ijk}\ |Q_{ijk}|_3$ è la somma superiore di $f$ su
  $Q$.
- $s(f,D) = \sum_{i, j, k} m_{ijk}\ |Q_{ijk}|_3$ è la somma inferiore di $f$ su
  $Q$.

Si dice che $f$ è integrabile secondo Riemann su $Q$ e si scrive $f \in R(Q)$
se:

$$
L = \sup_D s(f,D) = \inf_D S(f,D)
$$

Il valore $L$ prende il nome di integrale triplo di $f$ su $Q$ e si denota con:

$$
\iiint_Q f(x, y, z)\ dx\ dy\ dz
$$

:::note

Valgono gli stessi teoremi degli integrali doppi su rettangoli.

:::

### Integrale triplo su insiemi generali

Si applica la stessa definizione usata per gli integrali doppi. Si consideri un
parallelepipedo $Q$ che racchiude l'insieme e una funzione $\tilde{f}$ che vale
come $f$ all'interno dell'insieme e $0$ fuori.

#### Insiemi misurabili

Come per $\R^2$, $A \subseteq \R^3$ è misurabile se:

- $\partial A$ è misurabile;
- $|\partial A|_3 = 0$

## Cambio di variabile per integrali tripli

Siano $A, A' \subseteq \R^3$ insiemi aperti, limitati e misurabili. Sia
$\psi: A' \to A$ un cambiamento di variabili e sia $f \in C^0(A)$ e limitata.
Allora:

$$
\iiint_A f(x, y, z)\ dx\ dy\ dz = \iiint_{A'} f(\psi(u, v, w))\ |det(D_\psi(u, v, w))|\ du\ dv\ dw
$$
