---
description:
  Il teorema del valor medio per funzioni di più variabili, la matrice Jacobiana
  come estensione del gradiente e la regola della catena per funzioni composte.
lang: it
title: Teorema del valor medio e regola della catena
---

## Teorema del valor medio in $n$ variabili

:::tip[Interpolazione lineare]

$f(t) = t\ \mathbf{q} + (1 - t)\ \mathbf{p}$, con $f: [0, 1] \to \R^n$, è detta
interpolazione lineare tra i punti $\mathbf{p}$ e $\mathbf{q}$. Nei linguaggi di
programmazione, spesso la funzione è chiamata `lerp`.

L'immagine di $f$ è rappresentata dal segmento con estremi $\mathbf{p}$ e
$\mathbf{q}$.

:::

Consideriamo una funzione $f: A \subseteq \R^n \to \R$ definita su un insieme
$A$ tale che:

- Esistono $\mathbf{p}, \mathbf{q} \in A$ per cui il segmento
  $[\mathbf{p}, \mathbf{q}] = \Set{t\ \mathbf{q} + (1 - t)\ \mathbf{p}, t \in [0,1]}$
  è contenuto in $A$.
- $f$ è continua sul segmento $[\mathbf{p}, \mathbf{q}]$ e differenziabile al
  suo interno $(\mathbf{p}, \mathbf{q})$.

In queste condizioni, esiste un punto $\mathbf{c} \in (\mathbf{p}, \mathbf{q})$
tale che:

$$
f(\mathbf{q}) - f(\mathbf{p}) = \nabla f(\mathbf{c}) \cdot (\mathbf{q} - \mathbf{p})
$$

**Corollario**: Se $\forall\ \mathbf{p} \in B(\mathbf{p}_0, r)$, il gradiente è
nullo ($\nabla f(\mathbf{p}) = \mathbf{0}$), allora la funzione $f$ è costante
su quell'intorno, ovvero
$\forall\ \mathbf{p} \in B(\mathbf{p}_0, r),\ f(\mathbf{p}) = f(\mathbf{p}_0)$.

## Derivate parziali di una funzione composta di più variabili

:::note[Ripasso]

La regola della catena per una funzione di una variabile
$h(x) = (f \circ g)(x) = f(g(x))$ è data da: $h'(x) = f'(g(x))\ g'(x)$.

:::

### Matrice Jacobiana

Sia $f: A \subseteq \R^n \to \R^m$,
$f(\mathbf{x}) = (f_1(\mathbf{x}), \ldots, f_m(\mathbf{x}))$, con
$f_i: A \to \R$.

Assumendo che per ogni $i = 1,\ldots,m$, il gradiente parziale
$\nabla f_i(\mathbf{x})$ esista in $\mathbf{x}$, la matrice Jacobiana
$D_f(\mathbf{x})$ è una matrice di dimensione $m \times n$ la cui $i$-esima riga
è data da $\nabla f_i(\mathbf{x})$:

$$
D_f(\mathbf{x}) = \begin{bmatrix}
\frac{\partial f_1}{\partial x_1}(\mathbf{x}) & \ldots & \frac{\partial f_1}{\partial x_n}(\mathbf{x}) \\
\vdots & & \vdots \\
\frac{\partial f_m}{\partial x_1}(\mathbf{x}) & \ldots & \frac{\partial f_m}{\partial x_n}(\mathbf{x})
\end{bmatrix}
$$

:::note

La nozione di matrice Jacobiana generalizza la nozione di vettore gradiente per
funzioni scalari. Infatti ponendo $m = 1$ si ottiene il vettore del gradiente.

:::

### Regola della catena

La regola della catena permette di calcolare le derivate parziali di una
funzione composta, richiedendo solo il calcolo delle derivate parziali delle
funzioni componenti.

Siano $g: A \subseteq \R^n \to \R^m$, $f: B \subseteq \R^m \to \R^k$ e
supponiamo che $g(A) \subseteq B$.

Inoltre supponiamo che, dato un punto $\mathbf{x} \in A$:

- $g(\mathbf{x}) = (g_1(\mathbf{x}), \ldots, g_m(\mathbf{x}))$, con
  $g_i: A \to \R$ differenziabili in $\mathbf{x}$.
- $f(\mathbf{y}) = (f_1(\mathbf{y}), \ldots, f_k(\mathbf{y}))$, con
  $f_j: B \to \R$ differenziabili in $\mathbf{y} = g(\mathbf{x})$.

![Regola della catena](../../../../../images/analisi-2/regola-della-catena.png)

Consideriamo ora la funzione composta $h: \R^n \to \R^k$,
$h(\mathbf{x}) = (f \circ g)(\mathbf{x})$; allora le funzioni $h_1, \ldots, h_k$
sono differenziabili in $\mathbf{x}$ e la matrice Jacobiana di $h$ è data dal
prodotto delle matrici Jacobiane delle funzioni di partenza.

$$
D_h(\mathbf{x}) = D_f(g(\mathbf{x}))\ D_g(\mathbf{x})
$$
