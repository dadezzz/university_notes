---
description:
  Analisi di circuiti RLC nel dominio della frequenza utilizzando serie di
  Fourier per segnali periodici e la trasformata di Fourier per segnali
  generici.
lang: it
title: Serie di Fourier e trasformata nei circuiti
---

## Analisi di un circuito in frequenza

Nel dominio del tempo, gli unici tipi di ingresso per cui possiamo risolvere un
circuito RLC sono il gradino e l'impulso. La risposta sarà data dalla
convoluzione tra l'ingresso e la risposta impulsiva: $y(t) = x(t) * h(t)$.

Abbiamo poi introdotto il concetto di fasore, che permette di trovare la
soluzione per ingressi di tipo sinusoidale. In questo caso si può caratterizzare
il circuito con la funzione di trasferimento:
$tilde(Y) = tilde(H)(omega) tilde(X)$.

Il primo passo che possiamo fare verso una generalizzazione è quello delle serie
di Fourier, che ci permetteranno di analizzare la risposta di un circuito con un
ingresso **periodico** qualsiasi.

## Serie di Fourier

Consideriamo una funzione $f(x)$ periodica di periodo $T$ da $-oo$ a $+oo$.

La serie di Fourier di $f$ è definita:

$$
f(x) = a_0 / 2 + sum_(n = 1)^(+oo) a_n cos((2 pi n x) / T) + sum_(n = 1)^(+oo) b_n sin((2 pi n x) / T)
$$

dove:

$$
a_n = 2 / T integral_(-T / 2)^(T / 2) f(x) cos((2 pi n x) / T) dif x \
b_n = 2 / T integral_(-T / 2)^(T / 2) f(x) sin((2 pi n x) / T) dif x
$$

D'ora in poi $omega = (2 pi) / T$. La serie può essere riscritta usando le
formule di Eulero:

$$
f(x) &= a_0 / 2 + sum_(n = 1)^(+oo) a_n (e^(j n omega x) + e^(- j n omega x)) / 2 + b_n (e^(j n omega x) - e^(- j n omega x)) / (2 j) \
     &= a_0 / 2 + sum_(n = 1)^(+oo) a_n (e^(j n omega x) + e^(- j n omega x)) / 2 + b_n j / j (e^(j n omega x) - e^(- j n omega x)) / (2 j) \
     &= a_0 / 2 + sum_(n = 1)^(+oo) a_n (e^(j n omega x) + e^(- j n omega x)) / 2 - b_n j (e^(j n omega x) - e^(- j n omega x)) / 2 \
     &= a_0 / 2 + sum_(n = 1)^(+oo) (a_n - j b_n) / 2 e^(j n omega x) + (a_n + j b_n) / 2 e^(- j n omega x) \
     &= sum_(n = -oo)^(+oo) c_n e^(j n omega x)
$$

dove:

$$
c_n &= (a_n - j b_n) / 2 \
    &= 1 / T integral_(-T / 2)^(T / 2) f(x) cos(n omega x) dif x - j / T integral_(-T / 2)^(T / 2) f(x) sin(n omega x) dif x \
    &= 1 / T integral_(-T / 2)^(T / 2) f(x) (cos(n omega x) - j sin(n omega x)) dif x \
    &= 1 / T integral_(-T / 2)^(T / 2) f(x) e^(- j n omega x) dif x
$$

## Trasformata

Una trasformata è una funzione che moltiplica l'originale per un certo kernel,
restituendo una nuova funzione con un dominio diverso:

$$
g(alpha) = integral_a^b f(x) K(alpha, x) dif x
$$

Il kernel per la trasformata di Fourier è $K(alpha, x) = e^(- j alpha x)$.
Quindi diventa:

- trasformata di Fourier:

  $$
  tilde(F)(omega) = integral_(-oo)^(+oo) f(t) e^(- j omega t) dif t
  $$

- antitrasformata di Fourier:

  $$
  f(t) = 1 / (2 pi) integral_(-oo)^(+oo) tilde(F)(omega) e^(j omega t) dif t
  $$

:::note

Si adotta la **convenzione ingegneristica**: trasformata senza fattori,
antitrasformata con $1 / (2 pi)$. In questo modo il teorema della convoluzione
vale senza coefficienti aggiuntivi, come usato nella lezione successiva.

:::

:::caution

La trasformata esiste solo se l'integrale
$integral_(-oo)^(+oo) abs(f(t)) dif t < oo$. Le funzioni di questo tipo sono
dette assolutamente integrabili e si indicano come $f in L^1(bb(R))$.

È una condizione sufficiente ma non necessaria, ad esempio esiste la trasformata
di $cos(t)$ anche se il suo integrale diverge.

:::
